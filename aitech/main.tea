import AntchainUtil;
import Util;
import RPCUtil;

type @endpoint = string
type @regionId = string
type @accessKeyId = string
type @accessKeySecret = string
type @protocol = string
type @userAgent = string
type @readTimeout = number
type @connectTimeout = number
type @httpProxy = string
type @httpsProxy = string
type @socks5Proxy = string
type @socks5NetWork = string
type @noProxy = string
type @maxIdleConns = number
type @securityToken = string
type @maxIdleTimeMillis = number
type @keepAliveDurationMillis = number
type @maxRequests = number
type @maxRequestsPerHost = number

/**
 * Model for initing client
 */
model Config {
  accessKeyId?: string(description='accesskey id',default=''),
  accessKeySecret?: string(description='accesskey secret',default=''),
  securityToken?: string(description='security token',default=''),
  protocol?: string(description='http protocol',example='http',default='http'),
  readTimeout?: number(description='read timeout',example='10',default=''),
  connectTimeout?: number(description='connect timeout',example='10',default=''),
  httpProxy?: string(description='http proxy',example='http://localhost',default=''),
  httpsProxy?: string(description='https proxy',example='https://localhost',default=''),
  endpoint?: string(description='endpoint',example='cs.aliyuncs.com',default=''),
  noProxy?: string(description='proxy white list',example='http://localhost',default=''),
  maxIdleConns?: number(description='max idle conns',example='3',default=''),
  userAgent?: string(description='user agent',example='Alibabacloud/1',default=''),
  socks5Proxy?: string(description='socks5 proxy',default=''),
  socks5NetWork?: string(description='socks5 network',example='TCP',default=''),
  maxIdleTimeMillis?: number(description='长链接最大空闲时长',default='60 * 1000L'),
  keepAliveDurationMillis?: number(description='长链接最大连接时长',default='5000'),
  maxRequests?: number(description='最大连接数（长链接最大总数）',default='100'),
  maxRequestsPerHost?: number(description='每个目标主机的最大连接数（分主机域名的长链接最大总数',default='100'),
}

/**
 * Init client with Config
 * @param config config contains the necessary information to create a client
 */
init(config: Config) {
  if (Util.isUnset(config)) {
    throw {
      code = 'ParameterMissing',
      message = '\'config\' can not be unset'
    };
  }

  @accessKeyId = config.accessKeyId;
  @accessKeySecret = config.accessKeySecret;
  @securityToken = config.securityToken;
  @endpoint = config.endpoint;
  @protocol = config.protocol;
  @userAgent = config.userAgent;
  @readTimeout = Util.defaultNumber(config.readTimeout, 20000);
  @connectTimeout = Util.defaultNumber(config.connectTimeout, 20000);
  @httpProxy = config.httpProxy;
  @httpsProxy = config.httpsProxy;
  @noProxy = config.noProxy;
  @socks5Proxy = config.socks5Proxy;
  @socks5NetWork = config.socks5NetWork;
  @maxIdleConns = Util.defaultNumber(config.maxIdleConns, 60000);
  @maxIdleTimeMillis = Util.defaultNumber(config.maxIdleTimeMillis, 5);
  @keepAliveDurationMillis = Util.defaultNumber(config.keepAliveDurationMillis, 5000);
  @maxRequests = Util.defaultNumber(config.maxRequests, 100);
  @maxRequestsPerHost = Util.defaultNumber(config.maxRequestsPerHost, 100);
}

/**
 * Encapsulate the request and invoke the network
 * @param action api name
 * @param protocol http or https
 * @param method e.g. GET
 * @param pathname pathname of every api
 * @param request which contains request params
 * @param runtime which controls some details of call api, such as retry times
 * @return the response
 */
api doRequest(version: string, action: string, protocol: string, method: string, pathname: string, request: object, headers: map[string]string, runtime: Util.RuntimeOptions): object {
  __request.protocol = Util.defaultString(@protocol, protocol);
  __request.method = method;
  __request.pathname = pathname;
  __request.query = {
    method = action,
    version = version,
    sign_type = 'HmacSHA1',
    req_time = AntchainUtil.getTimestamp(),
    req_msg_id = AntchainUtil.getNonce(),
    access_key = @accessKeyId,
    base_sdk_version = 'TeaSDK-2.0',
    sdk_version = '1.1.60',
    _prod_code = 'AITECH',
    _prod_channel = 'default'
  };

  if (!Util.empty(@securityToken)) {
    __request.query.security_token = @securityToken;
  }

  __request.headers = {
    host = Util.defaultString(@endpoint, 'openapi.antchain.antgroup.com'),
    user-agent = Util.getUserAgent(@userAgent),
    ...headers
  };

  var tmp = Util.anyifyMapValue(RPCUtil.query(request));
  __request.body = Util.toFormString(tmp);
  __request.headers.content-type = 'application/x-www-form-urlencoded';

  var signedParam = {
    ...__request.query,
    ...RPCUtil.query(request)
  };
  __request.query.sign = AntchainUtil.getSignature(signedParam, @accessKeySecret);
} returns {
  var raw = Util.readAsString(__response.body);
  var obj = Util.parseJSON(raw);
  var res = Util.assertAsMap(obj);
  var resp = Util.assertAsMap(res.response);

  if (AntchainUtil.hasError(raw, @accessKeySecret)) {
    throw {
      message = resp.result_msg,
      data = resp,
      code = resp.result_code
    };
  }
  return resp;
} runtime {
  timeouted = 'retry',
  readTimeout = Util.defaultNumber(runtime.readTimeout, @readTimeout),
  connectTimeout = Util.defaultNumber(runtime.connectTimeout, @connectTimeout),
  httpProxy = Util.defaultString(runtime.httpProxy, @httpProxy),
  httpsProxy = Util.defaultString(runtime.httpsProxy, @httpsProxy),
  noProxy = Util.defaultString(runtime.noProxy, @noProxy),
  maxIdleConns = Util.defaultNumber(runtime.maxIdleConns, @maxIdleConns),
  maxIdleTimeMillis = @maxIdleTimeMillis,
  keepAliveDuration = @keepAliveDurationMillis,
  maxRequests = @maxRequests,
  maxRequestsPerHost = @maxRequestsPerHost,
  retry = {
    retryable = runtime.autoretry,
    maxAttempts = Util.defaultNumber(runtime.maxAttempts, 3)
  },
  backoff = {
    policy = Util.defaultString(runtime.backoffPolicy, 'no'),
    period = Util.defaultNumber(runtime.backoffPeriod, 1)
  },
  ignoreSSL = runtime.ignoreSSL
}

// 二维码详情
model QRCodeDetail {
  // 位置，Top、Left、Width、Height
  location: [ long ](name='location', example=''),
  // 二维码类型
  type: string(name='type', example='二维码类型'),
  // 二维码内容
  content: string(name='content', example='二维码内容'),
  // 置信度
  probability: string(name='probability', example='置信度'),
}

// Logo详情
model LogoDetail {
  // 位置，Top、Left、Width、Height
  location: [ long ](name='location', example=''),
  // 类型
  type: string(name='type', example='LOGO类型'),
  // 置信度
  probability: string(name='probability', example='置信度'),
}

// maya响应体
model MayaRedGptResponseDTO {
  // 消息的ID
  messageId: string(name='message_id', example='123123'),
  // 请求ID
  requestId: string(name='request_id', example='4564546'),
  // 会话ID
  sessionId: string(name='session_id', example='312414124'),
  // 应答内容
  answer: string(name='answer', example='你可以问我信息安全的问题'),
  // 应答内容格式
  answerFormat: string(name='answer_format', example='PLAINTEXT'),
  // 是否回答结束
  answerEnd: boolean(name='answer_end', example='true, false'),
  // 是否问题有风险
  safe: boolean(name='safe', example='true, false'),
}

// 审核同步标签列表
model AuditSyncLabel {
  // 标签名：sex-色情
  label: string(name='label', example='sex'),
  // 检测到单个风险标签的置信度：66.25
  probability: long(name='probability', example='66.25'),
  // 风险标签说明
  description?: string(name='description', example='疑似色情内容'),
  // 检测到的敏感词，多个词用逗号分隔，部分标签不会返回敏感词：AA,BB,CC
  riskWords: string(name='risk_words', example='AA,BB,CC'),
}

// QRCode审核结果
model QRCodeAuditResult {
  // 检测到二维码个数
  detectNum: long(name='detect_num', example=''),
  // 二维码详情
  details: [ QRCodeDetail ](name='details', example=''),
}

// 攻击手法二级标签
model AttackSubLabel {
  // 提示词攻击手法二级标签
  attackSubLabel?: string(name='attack_sub_label', example='role_play：角色扮演'),
}

// 主题信息
model MeiyouTopicWebInfo {
  // 内容文本
  content?: string(name='content', example='内容文本'),
  // 当前楼层
  currentFloor?: long(name='current_floor', example='1'),
  // 回复楼层
  callBackFloor?: long(name='call_back_floor', example='1'),
  // 发布时间戳(毫秒)
  // 
  publishTime?: long(name='publish_time', example=''),
  // 图片URL数组(JSON字符串)
  images?: string(name='images', example='{}'),
  // 用户昵称
  userNickname?: string(name='user_nickname', example='a'),
  // 用户ID
  userId?: string(name='user_id', example='1'),
  // 用户类型
  // 
  userType?: string(name='user_type', example='q'),
  // 用户头像URL
  userAvatar?: string(name='user_avatar', example='http://test'),
  // 主题ID
  topicId?: long(name='topic_id', example='1'),
  // 回复楼信息
  callBackFloorContent?: string(name='call_back_floor_content', example='test'),
  // oss存储地址
  ossImages?: string(name='oss_images', example='{}'),
}

// maya流式响应结果
model MayaStreamResult {
  // maya响应数据
  data: MayaRedGptResponseDTO(name='data', example=''),
  // 是否成功
  success: boolean(name='success', example='true, false'),
  // 错误码
  errorCode: string(name='error_code', example='10002'),
  // 错误信息
  errorMsg: string(name='error_msg', example='maya平台调用失败'),
}

// 二级标签结构
model SubLabelModel {
  // 二级标签
  subLabel: string(name='sub_label', example='正常'),
  // 风险关键词列表
  riskWords?: [ string ](name='risk_words', example=''),
  // 风险关键词索引列表
  riskWordsIndex?: [ string ](name='risk_words_index', example=''),
  // 三级标签列表
  thirdLabels?: [ string ](name='third_labels', example=''),
}

// logo审核结果
model LogoAuditResult {
  // 检测到LOGO个数
  detectNum: long(name='detect_num', example=''),
  // LOGO详情
  details: [ LogoDetail ](name='details', example=''),
}

// 领域信息
model FieldModel {
  // 领域一级标签
  fieldCategory?: string(name='field_category', example='金融'),
  // 领域二级标签
  fieldLabel?: string(name='field_label', example='金融'),
  // 领域一级标签的等级分数
  fieldScore?: long(name='field_score', example=''),
}

// 美柚审核信息存储请求
model MeiyouAuditSaveWebRequest {
  // 审核记录ID
  auditId?: long(name='audit_id', example='1'),
  // 主题ID
  topicId?: long(name='topic_id', example='1'),
  // 内容文本
  content?: string(name='content', example='test'),
  // 发布时间戳(毫秒)
  publishTime?: long(name='publish_time', example='1'),
  // 图片URL数组(JSON字符串)
  images?: string(name='images', example='{}'),
  // 一级业务
  primaryBusiness?: string(name='primary_business', example='test'),
  // 二级业务
  secondaryBusiness?: string(name='secondary_business', example='1'),
  // 用户昵称
  userNickname?: string(name='user_nickname', example='小蜜'),
  // 用户ID
  userId?: string(name='user_id', example='1'),
  // 用户类型
  userType?: string(name='user_type', example='INIT'),
  // 用户头像URL
  userAvatar?: string(name='user_avatar', example='http://test'),
  // 操作人
  auditOperator?: string(name='audit_operator', example='小蜜'),
  // 审核楼
  auditFloor?: long(name='audit_floor', example='1'),
  // 版本
  version?: string(name='version', example='1'),
  // 主题信息
  topicInfos?: [ MeiyouTopicWebInfo ](name='topic_infos', example='{}'),
}

// 网关响应模型
model AntCloudProdProviderHttpResponse {
  // maya返回结果
  response: MayaStreamResult(name='response', example=''),
  // 签名
  sign: string(name='sign', example='5Okl4F2SNc9L2zrWCF8xZ+QPUyA='),
}

// 一级标签信息
model LabelModel {
  // 一级标签
  label?: string(name='label', example='正常'),
  // 子标签
  subLabels?: [ SubLabelModel ](name='sub_labels', example=''),
}

// 文本同步审核结果
model TextSyncAuditResult {
  // 任务id
  taskId: string(name='task_id', example='123'),
  // 数据Id
  dataId: string(name='data_id', example='123'),
  // 业务Id - 调用方透传
  businessId: string(name='business_id', example='123'),
  // 风险等级，根据设置的高低风险分返回，返回值包括： - high：高风险（若命中自定义词库，风险等级默认为高风险） - medium：中风险 - low：低风险 - none：未检测到风险
  riskLevel: string(name='risk_level', example='high'),
  // 审核标签列表
  labels: [ AuditSyncLabel ](name='labels', example='[{"label":"sex", "probability":66.5, "description":"疑似色情内容", "riskWords":"AA,BB,CC"}]'),
}

// 美柚审核信息存储
model MeiyouAuditSaveWebInfo {
  // 主题ID
  topicId?: long(name='topic_id', example='1'),
  // 审核记录ID
  auditId: long(name='audit_id', example='1'),
  // 内容文本
  content?: string(name='content', example='小蜜'),
  // 发布时间戳(毫秒)
  publishTime?: long(name='publish_time', example=''),
  // 图片URL数组(JSON字符串)
  images?: string(name='images', example='{}'),
  // 一级业务
  primaryBusiness?: string(name='primary_business', example='一级业务'),
  // 二级业务
  secondaryBusiness?: string(name='secondary_business', example='二级业务'),
  // 用户昵称
  userNickname?: string(name='user_nickname', example='用户昵称'),
  // 用户ID
  userId?: string(name='user_id', example='用户ID'),
  // 用户类型
  // 
  userType?: string(name='user_type', example='用户类型'),
  // 用户头像URL
  userAvatar?: string(name='user_avatar', example='http://'),
  //  主题信息
  topicInfos?: MeiyouTopicWebInfo(name='topic_infos', example=' 主题信息'),
}

// 图片审核结果
model ImageAuditResult {
  // 任务ID
  taskId: string(name='task_id', example='任务ID'),
  // 数据ID
  dataId: string(name='data_id', example='数据ID'),
  // LOGO审核结果
  logoAuditResult: LogoAuditResult(name='logo_audit_result', example=''),
  // QRCode审核结果
  qrCodeAuditResult: QRCodeAuditResult(name='qr_code_audit_result', example=''),
}

// 提示词攻击手法一级标签
model AttackLabel {
  // 提示词攻击手法一级标签
  attackLabel?: string(name='attack_label', example='jailbreak：越狱攻击'),
  // 提示词攻击手法二级标签列表
  attackSubLabels?: AttackSubLabel(name='attack_sub_labels', example=''),
}

// 美柚审核信息
model MeiyouAuditInfo {
  // 主键id
  id: long(name='id', example='1'),
  // 审核记录ID
  auditId: long(name='audit_id', example='1'),
  // 主题ID
  topicId?: long(name='topic_id', example='1'),
  // 内容文本
  content?: string(name='content', example='内容文本'),
  // 发布时间戳
  publishTime?: string(pattern='\\\\d{4}[-]\\\\d{1,2}[-]\\\\d{1,2}[T]\\\\d{2}:\\\\d{2}:\\\\d{2}([Z]|([\\\\.]\\\\d{1,9})?[\\\\+]\\\\d{2}[\\\\:]?\\\\d{2})', name='publish_time', example='2018-10-10T10:10:00Z'),
  // 图片URL数组(JSON字符串)
  images?: string(name='images', example='{}'),
  // 一级业务
  primaryBusiness?: string(name='primary_business', example='1'),
  // 二级业务
  secondaryBusiness?: string(name='secondary_business', example='2'),
  // 用户昵称
  userNickname?: string(name='user_nickname', example='小蜜'),
  // 用户ID
  userId?: string(name='user_id', example='1'),
  // 用户类型
  userType?: string(name='user_type', example='test'),
  // 用户头像URL
  userAvatar?: string(name='user_avatar', example='http://test'),
  // 审核结果
  auditResult?: string(name='audit_result', example='OK'),
  // 审核原因
  auditReason?: string(name='audit_reason', example='30'),
  // 审核原因
  auditReasonMsg?: string(name='audit_reason_msg', example='test'),
  // 操作人
  auditOperator?: string(name='audit_operator', example='小蜜'),
  // 操作时间戳
  auditTime?: string(pattern='\\\\d{4}[-]\\\\d{1,2}[-]\\\\d{1,2}[T]\\\\d{2}:\\\\d{2}:\\\\d{2}([Z]|([\\\\.]\\\\d{1,9})?[\\\\+]\\\\d{2}[\\\\:]?\\\\d{2})', name='audit_time', example='2018-10-10T10:10:00Z'),
  // oss 转存后的图像地址
  ossImages?: string(name='oss_images', example='{}'),
  // 创建时间
  gmtCreate?: string(pattern='\\\\d{4}[-]\\\\d{1,2}[-]\\\\d{1,2}[T]\\\\d{2}:\\\\d{2}:\\\\d{2}([Z]|([\\\\.]\\\\d{1,9})?[\\\\+]\\\\d{2}[\\\\:]?\\\\d{2})', name='gmt_create', example='2018-10-10T10:10:00Z'),
  // 更新时间
  gmtModified?: string(pattern='\\\\d{4}[-]\\\\d{1,2}[-]\\\\d{1,2}[T]\\\\d{2}:\\\\d{2}:\\\\d{2}([Z]|([\\\\.]\\\\d{1,9})?[\\\\+]\\\\d{2}[\\\\:]?\\\\d{2})', name='gmt_modified', example='2018-10-10T10:10:00Z'),
}

// 更新美柚itag关系信息
model UpdateMeiyouItagRelationWebInfo {
  // 主键id
  id: long(name='id', example='1'),
  // 审核记录ID
  auditId?: string(name='audit_id', example='1'),
  // itag数据ID
  itagDataId?: long(name='itag_data_id', example='1'),
  // 美柚任务审核结果推送状态
  meiyouAuditState?: string(name='meiyou_audit_state', example='INIT'),
  // 审核不通过原因
  refuseReson?: string(name='refuse_reson', example='34'),
  // 审核不通过图片序号
  refuseImages?: string(name='refuse_images', example='["1","2"]'),
  // 美柚itag关联状态
  auditState?: string(name='audit_state', example='1'),
  // 美柚itag关联状态
  topicState?: string(name='topic_state', example='1'),
  // 审核结果
  auditResult?: string(name='audit_result', example='1'),
  // 审核人员
  auditUser?: string(name='audit_user', example='1'),
  // 审核时间
  auditTime?: long(name='audit_time', example='1'),
}

model ApplyAuditImageRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 审核内容链接
  url: string(name='url'),
  // 审核类型列表
  secTypes: [ string ](name='sec_types'),
  // 场景信息
  scene: string(name='scene'),
  // 数据Id,客户传入，唯一标识
  dataId: string(name='data_id'),
}

model ApplyAuditImageResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 任务ID，必须唯一
  taskId?: string(name='task_id'),
  // 数据ID，必须唯一
  dataId?: string(name='data_id'),
  // 审核内容类型
  contentType?: string(name='content_type'),
  // 审核结果
  result?: string(name='result'),
}

/**
 * Description: LOGO/二维码审核
 * Summary: LOGO/二维码审核
 */
async function applyAuditImage(request: ApplyAuditImageRequest): ApplyAuditImageResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return applyAuditImageEx(request, headers, runtime);
}

/**
 * Description: LOGO/二维码审核
 * Summary: LOGO/二维码审核
 */
async function applyAuditImageEx(request: ApplyAuditImageRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ApplyAuditImageResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.image.apply', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model CallbackSofaAuditRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 回执原文
  payload: string(name='payload'),
  // 回执场景
  bizScene: string(name='biz_scene'),
}

model CallbackSofaAuditResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 错误码
  errorCode?: string(name='error_code'),
  // 错误原因
  errorMessage?: string(name='error_message'),
}

/**
 * Description: 主站审核能力回调
 * Summary: 主站审核能力回调
 */
async function callbackSofaAudit(request: CallbackSofaAuditRequest): CallbackSofaAuditResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return callbackSofaAuditEx(request, headers, runtime);
}

/**
 * Description: 主站审核能力回调
 * Summary: 主站审核能力回调
 */
async function callbackSofaAuditEx(request: CallbackSofaAuditRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CallbackSofaAuditResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.sofa.audit.callback', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SubmitAuditTextRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 待人工审核的文本内容，最长不超过10000个字符（包含中文、英文和标点符号）
  content: string(name='content'),
  // 审核场景码，该接口固定输入BASE_TEXT_AUDIT，其他值无效
  scene: string(name='scene'),
  // 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  dataId: string(name='data_id'),
  // 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
  // 结果通知地址，不指定时需要调用方主动查询结果
  callback?: string(name='callback'),
  // 传callback时必须指定，tenant + seed + auditResult做SHA256生成checksum，保证结果未被篡改（即数科官网控制台-账户信息中的「用户code」）
  seed?: string(name='seed'),
  // 审核时输入的额外信息
  extendInfo?: string(name='extend_info'),
}

model SubmitAuditTextResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 任务ID
  taskId?: string(name='task_id'),
  // 检测对象对应的数据ID
  dataId?: string(name='data_id'),
}

/**
 * Description: 文本审核入审
 * Summary: 文本审核入审
 */
async function submitAuditText(request: SubmitAuditTextRequest): SubmitAuditTextResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return submitAuditTextEx(request, headers, runtime);
}

/**
 * Description: 文本审核入审
 * Summary: 文本审核入审
 */
async function submitAuditTextEx(request: SubmitAuditTextRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SubmitAuditTextResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.text.submit', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryAuditTextRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 任务ID
  taskId: string(name='task_id'),
}

model QueryAuditTextResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 文本审核结果
  result?: string(name='result'),
}

/**
 * Description: 文本审核查询
 * Summary: 文本审核查询
 */
async function queryAuditText(request: QueryAuditTextRequest): QueryAuditTextResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryAuditTextEx(request, headers, runtime);
}

/**
 * Description: 文本审核查询
 * Summary: 文本审核查询
 */
async function queryAuditTextEx(request: QueryAuditTextRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryAuditTextResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.text.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SubmitAuditImageRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 待检测对象的URL，请确保该URL能通过公网访问到，且URL地址长度不超过2048个字符
  // URL地址中不能包含中文，且一次请求请确保仅传入1条URL
  url: string(name='url'),
  // 审核场景码，该接口固定输入BASE_IMAGE_AUDIT，其他值无效
  scene: string(name='scene'),
  // 检测对象对应的数据ID。
  // 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  dataId: string(name='data_id'),
  // 客户业务ID。
  // 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
  // 结果通知地址，不指定时需要调用方主动查询结果
  callback?: string(name='callback'),
  // 传callback时必须指定，tenant + seed + auditResult做SHA256生成checksum，保证结果未被篡改（即数科官网控制台-账户信息中的「用户code」）
  seed?: string(name='seed'),
  // 审核时输入的额外信息
  extendInfo?: string(name='extend_info'),
}

model SubmitAuditImageResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 任务ID
  taskId?: string(name='task_id'),
  // 检测对象对应的数据ID。
  // 如果在提交审核任务的请求参数中传入了dataId，则此处返回对应dataId
  dataId?: string(name='data_id'),
}

/**
 * Description: 图片审核入审
 * Summary: 图片审核入审
 */
async function submitAuditImage(request: SubmitAuditImageRequest): SubmitAuditImageResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return submitAuditImageEx(request, headers, runtime);
}

/**
 * Description: 图片审核入审
 * Summary: 图片审核入审
 */
async function submitAuditImageEx(request: SubmitAuditImageRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SubmitAuditImageResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.image.submit', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryAuditImageRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 任务ID
  taskId: string(name='task_id'),
}

model QueryAuditImageResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 图片审核结果
  result?: string(name='result'),
}

/**
 * Description: 图片审核查询
 * Summary: 图片审核查询
 */
async function queryAuditImage(request: QueryAuditImageRequest): QueryAuditImageResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryAuditImageEx(request, headers, runtime);
}

/**
 * Description: 图片审核查询
 * Summary: 图片审核查询
 */
async function queryAuditImageEx(request: QueryAuditImageRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryAuditImageResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.image.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SubmitAuditAudioRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 待检测对象的URL，请确保该URL能通过公网访问到，且URL地址长度不超过2048个字符
  // 说明 ：
  // URL地址中不能包含中文，且一次请求请确保仅传入1条URL
  url: string(name='url'),
  // 审核场景码，该接口固定输入BASE_AUDIO_AUDIT，其他值无效
  scene: string(name='scene'),
  // 检测对象对应的数据ID。
  // 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  dataId: string(name='data_id'),
  // 客户业务ID。
  // 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
  // 结果通知地址，不指定时需要调用方主动查询结果
  callback?: string(name='callback'),
  // 传callback时必须指定，tenant + seed + auditResult做SHA256生成checksum，保证结果未被篡改（即数科官网控制台-账户信息中的「用户code」）
  seed?: string(name='seed'),
  // 审核时输入的额外信息
  extendInfo?: string(name='extend_info'),
}

model SubmitAuditAudioResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 任务ID
  taskId?: string(name='task_id'),
  // 检测对象对应的数据ID。
  // 如果在提交审核任务的请求参数中传入了dataId，则此处返回对应dataId
  dataId?: string(name='data_id'),
}

/**
 * Description: 音频审核入审
 * Summary: 音频审核入审
 */
async function submitAuditAudio(request: SubmitAuditAudioRequest): SubmitAuditAudioResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return submitAuditAudioEx(request, headers, runtime);
}

/**
 * Description: 音频审核入审
 * Summary: 音频审核入审
 */
async function submitAuditAudioEx(request: SubmitAuditAudioRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SubmitAuditAudioResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.audio.submit', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryAuditAudioRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 任务ID
  taskId: string(name='task_id'),
}

model QueryAuditAudioResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 音频审核结果
  result?: string(name='result'),
}

/**
 * Description: 音频审核查询
 * Summary: 音频审核查询
 */
async function queryAuditAudio(request: QueryAuditAudioRequest): QueryAuditAudioResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryAuditAudioEx(request, headers, runtime);
}

/**
 * Description: 音频审核查询
 * Summary: 音频审核查询
 */
async function queryAuditAudioEx(request: QueryAuditAudioRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryAuditAudioResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.audio.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SubmitAuditVideoRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 待检测对象的URL，请确保该URL能通过公网访问到，且URL地址长度不超过2048个字符
  // URL地址中不能包含中文，且一次请求请确保仅传入1条URL
  url: string(name='url'),
  // 审核场景码，该接口固定输入BASE_VIDEO_AUDIT，其他值无效
  scene: string(name='scene'),
  // 检测对象对应的数据ID。
  // 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  dataId: string(name='data_id'),
  // 客户业务ID。
  // 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
  // 结果通知地址，不指定时需要调用方主动查询结果
  callback?: string(name='callback'),
  // 传callback时必须指定，tenant + seed + auditResult做SHA256生成checksum，保证结果未被篡改（即数科官网控制台-账户信息中的「用户code」）
  seed?: string(name='seed'),
  // 审核时输入的额外信息
  extendInfo?: string(name='extend_info'),
}

model SubmitAuditVideoResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 任务ID
  taskId?: string(name='task_id'),
  // 检测对象对应的数据ID。
  // 如果在提交审核任务的请求参数中传入了dataId，则此处返回对应dataId
  dataId?: string(name='data_id'),
}

/**
 * Description: 视频审核入审
 * Summary: 视频审核入审
 */
async function submitAuditVideo(request: SubmitAuditVideoRequest): SubmitAuditVideoResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return submitAuditVideoEx(request, headers, runtime);
}

/**
 * Description: 视频审核入审
 * Summary: 视频审核入审
 */
async function submitAuditVideoEx(request: SubmitAuditVideoRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SubmitAuditVideoResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.video.submit', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryAuditVideoRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 任务ID
  taskId: string(name='task_id'),
}

model QueryAuditVideoResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 视频审核结果
  result?: string(name='result'),
}

/**
 * Description: 视频审核查询
 * Summary: 视频审核查询
 */
async function queryAuditVideo(request: QueryAuditVideoRequest): QueryAuditVideoResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryAuditVideoEx(request, headers, runtime);
}

/**
 * Description: 视频审核查询
 * Summary: 视频审核查询
 */
async function queryAuditVideoEx(request: QueryAuditVideoRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryAuditVideoResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.video.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model ApplyAuditTextRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 待人工审核的文本内容，最长不超过10000个字符（包含中文、英文和标点符号）
  content: string(name='content'),
  // 审核场景码，建议BASE_TEXT_SEC代指阿里云，文本审核增强版PLUS服务的某一个Service
  scene: string(name='scene'),
  // 检测对象对应的数据ID，由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  dataId: string(name='data_id'),
  // 客户业务ID，由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
}

model ApplyAuditTextResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 文本同步审核结果，字段含义见：TextSyncAuditResult
  result?: string(name='result'),
}

/**
 * Description: 文本同步审核接口，调用阿里云内容安全审核增强版
 * Summary: 文本同步审核
 */
async function applyAuditText(request: ApplyAuditTextRequest): ApplyAuditTextResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return applyAuditTextEx(request, headers, runtime);
}

/**
 * Description: 文本同步审核接口，调用阿里云内容安全审核增强版
 * Summary: 文本同步审核
 */
async function applyAuditTextEx(request: ApplyAuditTextRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ApplyAuditTextResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.text.apply', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model ApplyAuditImagebaseRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 审核内容链接
  // 目前支持 jpeg、png、jpg、bmp、webp 五种格式
  // 图片的 contentType 必须为 image/xxx 或者 application/octet-stream，其余类型都无法识别
  url: string(name='url'),
  // 场景，固定填写：BASE_IMAGE_SEC
  scene: string(name='scene'),
  // 数据Id，调用方入审数据的唯一Id
  dataId: string(name='data_id'),
  // 客户业务ID
  // 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
}

model ApplyAuditImagebaseResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 图片基础版审核结果
  result?: string(name='result'),
}

/**
 * Description: 同步接口，只调用机审能力
 * Summary: 图片机审基础版
 */
async function applyAuditImagebase(request: ApplyAuditImagebaseRequest): ApplyAuditImagebaseResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return applyAuditImagebaseEx(request, headers, runtime);
}

/**
 * Description: 同步接口，只调用机审能力
 * Summary: 图片机审基础版
 */
async function applyAuditImagebaseEx(request: ApplyAuditImagebaseRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ApplyAuditImagebaseResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.imagebase.apply', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model ApplyAuditImageadvancedRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 审核内容链接
  // 目前支持 jpeg、png、jpg、bmp、webp 五种格式
  // 图片的 contentType 必须为 image/xxx 或者 application/octet-stream，其余类型都无法识别
  url: string(name='url'),
  // 场景，固定填写：ADVANCED_IMAGE_SEC
  scene: string(name='scene'),
  // 数据Id，调用方入审数据的唯一Id
  dataId: string(name='data_id'),
  // 客户业务ID
  // 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
}

model ApplyAuditImageadvancedResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 图片增强版审核结果
  result?: string(name='result'),
}

/**
 * Description: 同步接口，只调用机审能力
 * Summary: 图片审核增强版
 */
async function applyAuditImageadvanced(request: ApplyAuditImageadvancedRequest): ApplyAuditImageadvancedResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return applyAuditImageadvancedEx(request, headers, runtime);
}

/**
 * Description: 同步接口，只调用机审能力
 * Summary: 图片审核增强版
 */
async function applyAuditImageadvancedEx(request: ApplyAuditImageadvancedRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ApplyAuditImageadvancedResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.imageadvanced.apply', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SubmitAuditAudiobaseRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 待检测对象的URL，请确保该URL能通过公网访问到，且URL地址长度不超过2048个字符
  // 说明 ：
  // 1. URL地址中不能包含中文，且一次请求请确保仅传入1条URL
  // 2. 支持音频文件格式：MP3、WAV、AAC、WMA、OGG、M4A、AMR
  url: string(name='url'),
  // 审核场景类型
  // 目前支持 BASE_AUDIO_SEC：音视频媒体通用检测
  scene: string(name='scene'),
  // 检测对象对应的数据ID。 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  dataId: string(name='data_id'),
  // 客户业务ID
  // 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
  // 结果通知地址，不指定时需要调用方主动查询结果
  callback?: string(name='callback'),
  // 传callback时必须指定，tenant + seed + auditResult做SHA256生成checksum，保证结果未被篡改（即数科官网控制台-账户信息中的「用户code」）
  seed?: string(name='seed'),
}

model SubmitAuditAudiobaseResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 任务ID
  taskId?: string(name='task_id'),
  // 检测对象对应的数据ID。 如果在提交审核任务的请求参数中传入了dataId，则此处返回对应dataId
  dataId?: string(name='data_id'),
}

/**
 * Description: 异步接口，只调用机审能力
 * Summary: 音频审核通用版入审
 */
async function submitAuditAudiobase(request: SubmitAuditAudiobaseRequest): SubmitAuditAudiobaseResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return submitAuditAudiobaseEx(request, headers, runtime);
}

/**
 * Description: 异步接口，只调用机审能力
 * Summary: 音频审核通用版入审
 */
async function submitAuditAudiobaseEx(request: SubmitAuditAudiobaseRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SubmitAuditAudiobaseResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.audiobase.submit', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryAuditAudiobaseRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 任务ID
  taskId: string(name='task_id'),
}

model QueryAuditAudiobaseResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 音频通用版审核结果
  // 
  result?: string(name='result'),
}

/**
 * Description: 异步接口，只调用机审能力
 * Summary: 音频审核通用版查询
 */
async function queryAuditAudiobase(request: QueryAuditAudiobaseRequest): QueryAuditAudiobaseResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryAuditAudiobaseEx(request, headers, runtime);
}

/**
 * Description: 异步接口，只调用机审能力
 * Summary: 音频审核通用版查询
 */
async function queryAuditAudiobaseEx(request: QueryAuditAudiobaseRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryAuditAudiobaseResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.audiobase.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SubmitAuditVideobaseRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 待检测对象的URL，请确保该URL能通过公网访问到，且URL地址长度不超过2048个字符
  // 说明 ：
  // 1. URL地址中不能包含中文，且一次请求请确保仅传入1条URL
  // 2. 视频文件链接支持以下协议：HTTP和HTTPS。
  // 3. 视频文件支持以下格式：AVI、FLV、MP4、MPG、ASF、WMV、MOV、WMA、RMVB、RM、FLASH、TS。
  // 4. 视频大小限制：默认单个视频大小不超过500 MB。如果您的需求超过500 MB，您可以对视频进行分片处理
  url: string(name='url'),
  // 审核场景类型
  // 目前支持通用版 BASE_VIDEO_SEC
  scene: string(name='scene'),
  // 检测对象对应的数据ID。 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  dataId: string(name='data_id'),
  // 客户业务ID 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
  // 结果通知地址，不指定时需要调用方主动查询结果
  callback?: string(name='callback'),
  // 传callback时必须指定，tenant + seed + auditResult做SHA256生成checksum，保证结果未被篡改（即数科官网控制台-账户信息中的「用户code」）
  seed?: string(name='seed'),
}

model SubmitAuditVideobaseResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 任务ID
  taskId?: string(name='task_id'),
  // 检测对象对应的数据ID。 如果在提交审核任务的请求参数中传入了dataId，则此处返回对应dataId
  dataId?: string(name='data_id'),
}

/**
 * Description: 异步通知，只调用机审能力
 * Summary: 视频审核通用版入审
 */
async function submitAuditVideobase(request: SubmitAuditVideobaseRequest): SubmitAuditVideobaseResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return submitAuditVideobaseEx(request, headers, runtime);
}

/**
 * Description: 异步通知，只调用机审能力
 * Summary: 视频审核通用版入审
 */
async function submitAuditVideobaseEx(request: SubmitAuditVideobaseRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SubmitAuditVideobaseResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.videobase.submit', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryAuditVideobaseRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 任务ID
  taskId: string(name='task_id'),
}

model QueryAuditVideobaseResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 视频通用版审核结果
  result?: string(name='result'),
}

/**
 * Description: 异步查询
 * Summary: 视频审核通用版查询
 */
async function queryAuditVideobase(request: QueryAuditVideobaseRequest): QueryAuditVideobaseResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryAuditVideobaseEx(request, headers, runtime);
}

/**
 * Description: 异步查询
 * Summary: 视频审核通用版查询
 */
async function queryAuditVideobaseEx(request: QueryAuditVideobaseRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryAuditVideobaseResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.videobase.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model CallbackAliyunAuditRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 回执原文
  payload: string(name='payload'),
  // 回执场景，固定填写 ALIYUN"
  bizScene: string(name='biz_scene'),
}

model CallbackAliyunAuditResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 错误码
  errorCode?: string(name='error_code'),
  // 错误原因
  errorMessage?: string(name='error_message'),
}

/**
 * Description: 阿里云音频、视频的审核结果，经过 adapter 中转，通过该接口回调回来
 * Summary: 阿里云内容审核回调
 */
async function callbackAliyunAudit(request: CallbackAliyunAuditRequest): CallbackAliyunAuditResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return callbackAliyunAuditEx(request, headers, runtime);
}

/**
 * Description: 阿里云音频、视频的审核结果，经过 adapter 中转，通过该接口回调回来
 * Summary: 阿里云内容审核回调
 */
async function callbackAliyunAuditEx(request: CallbackAliyunAuditRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CallbackAliyunAuditResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.aliyun.audit.callback', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model TransferAuditFileRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 客户文件路径
  url: string(name='url'),
}

model TransferAuditFileResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 响应信息
  result?: string(name='result'),
}

/**
 * Description: 外部文件转存
 * Summary: 文件转存接口
 */
async function transferAuditFile(request: TransferAuditFileRequest): TransferAuditFileResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return transferAuditFileEx(request, headers, runtime);
}

/**
 * Description: 外部文件转存
 * Summary: 文件转存接口
 */
async function transferAuditFileEx(request: TransferAuditFileRequest, headers: map[string]string, runtime: Util.RuntimeOptions): TransferAuditFileResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.file.transfer', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model DownloadAuditFileRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 文件地址
  url: string(name='url'),
}

model DownloadAuditFileResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 响应信息
  result?: string(name='result'),
}

/**
 * Description: 文件下载接口
 * Summary: 文件下载接口
 */
async function downloadAuditFile(request: DownloadAuditFileRequest): DownloadAuditFileResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return downloadAuditFileEx(request, headers, runtime);
}

/**
 * Description: 文件下载接口
 * Summary: 文件下载接口
 */
async function downloadAuditFileEx(request: DownloadAuditFileRequest, headers: map[string]string, runtime: Util.RuntimeOptions): DownloadAuditFileResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.file.download', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SaveAuditMeiyouRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 美柚待审核信息保存请求
  request: [ MeiyouAuditSaveWebInfo ](name='request'),
}

model SaveAuditMeiyouResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 码值
  errorCode?: string(name='error_code'),
  // 错误信息
  errorMessage?: string(name='error_message'),
  // 请求ID
  requestId?: string(name='request_id'),
  // 响应结果
  result?: string(name='result'),
}

/**
 * Description: 美柚待审核信息保存
 * Summary: 美柚待审核信息保存接口
 */
async function saveAuditMeiyou(request: SaveAuditMeiyouRequest): SaveAuditMeiyouResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return saveAuditMeiyouEx(request, headers, runtime);
}

/**
 * Description: 美柚待审核信息保存
 * Summary: 美柚待审核信息保存接口
 */
async function saveAuditMeiyouEx(request: SaveAuditMeiyouRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SaveAuditMeiyouResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.meiyou.save', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model UpdateAuditMeiyouRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 审核记录ID
  auditId: long(name='audit_id'),
  // 审核结果
  auditResult?: string(name='audit_result'),
  // 审核原因
  auditReason?: string(name='audit_reason'),
  // 操作人
  auditOperator?: string(name='audit_operator'),
  // 操作时间戳(毫秒)
  auditTime?: long(name='audit_time'),
}

model UpdateAuditMeiyouResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 异常码值
  errorCode?: string(name='error_code'),
  // 异常信息
  errorMessage?: string(name='error_message'),
  // 请求ID
  requestId?: string(name='request_id'),
  // 响应结果
  result?: string(name='result'),
}

/**
 * Description: 美柚审核信息存储请求
 * Summary: 美柚审核信息存储请求
 */
async function updateAuditMeiyou(request: UpdateAuditMeiyouRequest): UpdateAuditMeiyouResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return updateAuditMeiyouEx(request, headers, runtime);
}

/**
 * Description: 美柚审核信息存储请求
 * Summary: 美柚审核信息存储请求
 */
async function updateAuditMeiyouEx(request: UpdateAuditMeiyouRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UpdateAuditMeiyouResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.meiyou.update', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryMeiyouAuditRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 审核记录ID
  auditIds?: [ long ](name='audit_ids'),
  // 美柚itag关联状态
  auditState: string(name='audit_state'),
  // 开始时间
  gmtCreateStart?: string(pattern='\\\\d{4}[-]\\\\d{1,2}[-]\\\\d{1,2}[T]\\\\d{2}:\\\\d{2}:\\\\d{2}([Z]|([\\\\.]\\\\d{1,9})?[\\\\+]\\\\d{2}[\\\\:]?\\\\d{2})', name='gmt_create_start'),
  // 结束时间
  gmtCreateEnd?: string(pattern='\\\\d{4}[-]\\\\d{1,2}[-]\\\\d{1,2}[T]\\\\d{2}:\\\\d{2}:\\\\d{2}([Z]|([\\\\.]\\\\d{1,9})?[\\\\+]\\\\d{2}[\\\\:]?\\\\d{2})', name='gmt_create_end'),
  // 最大条数
  maxNum?: long(name='max_num'),
  // 数据来源
  source?: string(name='source'),
  // 操作人
  auditOperators?: [ string ](name='audit_operators'),
}

model QueryMeiyouAuditResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 响应信息
  result?: string(name='result'),
}

/**
 * Description: 美柚审核信息查询接口
 * Summary: 美柚审核信息查询接口
 */
async function queryMeiyouAudit(request: QueryMeiyouAuditRequest): QueryMeiyouAuditResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryMeiyouAuditEx(request, headers, runtime);
}

/**
 * Description: 美柚审核信息查询接口
 * Summary: 美柚审核信息查询接口
 */
async function queryMeiyouAuditEx(request: QueryMeiyouAuditRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryMeiyouAuditResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.meiyou.audit.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryMeiyouAudittopicRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 主题ID
  topicIds: [ long ](name='topic_ids'),
  // 美柚itag关联状态
  topicState: string(name='topic_state'),
  // 数据来源
  source?: string(name='source'),
}

model QueryMeiyouAudittopicResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 接口响应
  result?: string(name='result'),
}

/**
 * Description: 美柚主题信息查询接口
 * Summary: 美柚主题信息查询接口
 */
async function queryMeiyouAudittopic(request: QueryMeiyouAudittopicRequest): QueryMeiyouAudittopicResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryMeiyouAudittopicEx(request, headers, runtime);
}

/**
 * Description: 美柚主题信息查询接口
 * Summary: 美柚主题信息查询接口
 */
async function queryMeiyouAudittopicEx(request: QueryMeiyouAudittopicRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryMeiyouAudittopicResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.meiyou.audittopic.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model UpdateMeiyouAuditRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 主题ID
  topicIds?: [ long ](name='topic_ids'),
  // 美柚itag关联状态
  topicState?: string(name='topic_state'),
  // 审核记录ID
  auditIds?: [ long ](name='audit_ids'),
  // 美柚itag关联状态
  auditState?: string(name='audit_state'),
  // itag任务ID
  itagTaskId?: long(name='itag_task_id'),
  // itag数据集ID
  itagDatasetId?: long(name='itag_dataset_id'),
  // 数据来源
  source?: string(name='source'),
}

model UpdateMeiyouAuditResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 接口响应
  result?: string(name='result'),
}

/**
 * Description: 更新美柚itag关联关系接口
 * Summary: 更新美柚itag关联关系接口
 */
async function updateMeiyouAudit(request: UpdateMeiyouAuditRequest): UpdateMeiyouAuditResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return updateMeiyouAuditEx(request, headers, runtime);
}

/**
 * Description: 更新美柚itag关联关系接口
 * Summary: 更新美柚itag关联关系接口
 */
async function updateMeiyouAuditEx(request: UpdateMeiyouAuditRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UpdateMeiyouAuditResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.meiyou.audit.update', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryMeiyouItagrelationRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 主键id
  id?: long(name='id'),
  // 审核记录ID
  auditId?: long(name='audit_id'),
  // 主题ID
  topicId?: long(name='topic_id'),
  // itag任务ID
  itagTaskId?: long(name='itag_task_id'),
  // itag数据集ID
  itagDatasetId?: long(name='itag_dataset_id'),
  // itag数据ID
  itagDataId?: long(name='itag_data_id'),
  // 美柚任务审核结果推送状态
  meiyouAuditState?: string(name='meiyou_audit_state'),
  // 审核记录状态
  auditState?: string(name='audit_state'),
  // 主题记录状态
  topicState?: string(name='topic_state'),
  // 更新时间-start
  gmtModifiedStart?: string(pattern='\\\\d{4}[-]\\\\d{1,2}[-]\\\\d{1,2}[T]\\\\d{2}:\\\\d{2}:\\\\d{2}([Z]|([\\\\.]\\\\d{1,9})?[\\\\+]\\\\d{2}[\\\\:]?\\\\d{2})', name='gmt_modified_start'),
  // 更新时间-end 
  gmtModifiedEnd?: string(pattern='\\\\d{4}[-]\\\\d{1,2}[-]\\\\d{1,2}[T]\\\\d{2}:\\\\d{2}:\\\\d{2}([Z]|([\\\\.]\\\\d{1,9})?[\\\\+]\\\\d{2}[\\\\:]?\\\\d{2})', name='gmt_modified_end'),
  // 操作人
  auditOperator?: string(name='audit_operator'),
  // 数据来源
  source?: string(name='source'),
}

model QueryMeiyouItagrelationResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 响应
  result?: string(name='result'),
}

/**
 * Description: 查询美柚itag关联信息
 * Summary: 查询美柚itag关联信息
 */
async function queryMeiyouItagrelation(request: QueryMeiyouItagrelationRequest): QueryMeiyouItagrelationResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryMeiyouItagrelationEx(request, headers, runtime);
}

/**
 * Description: 查询美柚itag关联信息
 * Summary: 查询美柚itag关联信息
 */
async function queryMeiyouItagrelationEx(request: QueryMeiyouItagrelationRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryMeiyouItagrelationResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.meiyou.itagrelation.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model UpdateMeiyouItagrelationRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 更新关系信息
  relationInfoList?: [ UpdateMeiyouItagRelationWebInfo ](name='relation_info_list'),
  // 是否更新审核记录信息
  updateAuditFlag: boolean(name='update_audit_flag'),
  // 数据来源
  source?: string(name='source'),
}

model UpdateMeiyouItagrelationResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 响应结果
  result?: string(name='result'),
}

/**
 * Description: 更新美柚itag关联信息
 * Summary: 更新美柚itag关联信息
 */
async function updateMeiyouItagrelation(request: UpdateMeiyouItagrelationRequest): UpdateMeiyouItagrelationResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return updateMeiyouItagrelationEx(request, headers, runtime);
}

/**
 * Description: 更新美柚itag关联信息
 * Summary: 更新美柚itag关联信息
 */
async function updateMeiyouItagrelationEx(request: UpdateMeiyouItagrelationRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UpdateMeiyouItagrelationResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.meiyou.itagrelation.update', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryMeiyouAuditresultRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 审核ID
  auditIds?: [ long ](name='audit_ids'),
  // 美柚任务审核结果推送状态
  meiyouAuditState?: string(name='meiyou_audit_state'),
  // 审核记录状态
  auditState?: [ string ](name='audit_state'),
  // 主题记录状态
  topicState?: string(name='topic_state'),
  // 开始时间点
  createStart?: string(name='create_start'),
  // 结束时间点
  createEnd?: string(name='create_end'),
  // 数据来源
  source?: string(name='source'),
}

model QueryMeiyouAuditresultResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 响应信息
  result?: string(name='result'),
}

/**
 * Description: 美柚审核结果查询
 * Summary: 美柚审核结果查询
 */
async function queryMeiyouAuditresult(request: QueryMeiyouAuditresultRequest): QueryMeiyouAuditresultResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryMeiyouAuditresultEx(request, headers, runtime);
}

/**
 * Description: 美柚审核结果查询
 * Summary: 美柚审核结果查询
 */
async function queryMeiyouAuditresultEx(request: QueryMeiyouAuditresultRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryMeiyouAuditresultResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.meiyou.auditresult.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model PagequeryMeiyouAuditRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 主键id
  id?: long(name='id'),
  // 页面
  pageNum: long(name='page_num'),
  // 审核记录ID
  auditId?: long(name='audit_id'),
  // 大小
  pageSize: long(name='page_size'),
  // 主题ID
  topicId?: long(name='topic_id'),
  // 内容文本
  content?: string(name='content'),
  // 一级业务
  primaryBusiness?: string(name='primary_business'),
  // 二级业务
  secondaryBusiness?: string(name='secondary_business'),
  // 审核结果
  auditResult?: string(name='audit_result'),
  // 审核原因
  auditReason?: string(name='audit_reason'),
  // 创建时间-start
  gmtCreateStart?: string(name='gmt_create_start'),
  // 创建时间-end
  gmtCreateEnd?: string(name='gmt_create_end'),
  // 数据来源
  source?: string(name='source'),
}

model PagequeryMeiyouAuditResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 响应信息
  result?: string(name='result'),
}

/**
 * Description: 分页查询美柚审核信息接口
 * Summary: 分页查询美柚审核信息接口
 */
async function pagequeryMeiyouAudit(request: PagequeryMeiyouAuditRequest): PagequeryMeiyouAuditResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return pagequeryMeiyouAuditEx(request, headers, runtime);
}

/**
 * Description: 分页查询美柚审核信息接口
 * Summary: 分页查询美柚审核信息接口
 */
async function pagequeryMeiyouAuditEx(request: PagequeryMeiyouAuditRequest, headers: map[string]string, runtime: Util.RuntimeOptions): PagequeryMeiyouAuditResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.meiyou.audit.pagequery', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SubmitAuditMeiyouRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 数据来源
  source: string(name='source'),
  // 美柚审核信息存储请求
  saveInfo?: [ MeiyouAuditSaveWebRequest ](name='save_info'),
}

model SubmitAuditMeiyouResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 响应
  result?: string(name='result'),
}

/**
 * Description: 美柚审核信息存储（仅itask）
 * Summary: 美柚审核信息存储（仅itask）
 */
async function submitAuditMeiyou(request: SubmitAuditMeiyouRequest): SubmitAuditMeiyouResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return submitAuditMeiyouEx(request, headers, runtime);
}

/**
 * Description: 美柚审核信息存储（仅itask）
 * Summary: 美柚审核信息存储（仅itask）
 */
async function submitAuditMeiyouEx(request: SubmitAuditMeiyouRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SubmitAuditMeiyouResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.meiyou.submit', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SubmitAuditDocumentRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 待检测对象的URL，请确保该URL能通过公网访问到，且URL地址长度不超过2048个字符 URL地址中不能包含中文，且一次请求请确保仅传入1条URL
  url: string(name='url'),
  // 审核场景码
  scene: string(name='scene'),
  // 检测对象对应的数据ID。 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  dataId: string(name='data_id'),
  // 客户业务ID。 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
  // 结果通知地址，不指定时需要调用方主动查询结果
  callback?: string(name='callback'),
  // 传callback时必须指定，tenant + seed + auditResult做SHA256生成checksum，保证结果未被篡改（即数科官网控制台-账户信息中的「用户code」）
  seed?: string(name='seed'),
  // 审核时输入的额外信息
  extendInfo?: string(name='extend_info'),
}

model SubmitAuditDocumentResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 任务ID
  taskId?: string(name='task_id'),
  // 检测对象对应的数据ID。 如果在提交审核任务的请求参数中传入了dataId，则此处返回对应dataId
  dataId?: string(name='data_id'),
}

/**
 * Description: 文档人审入审
 * Summary: 文档人审入审
 */
async function submitAuditDocument(request: SubmitAuditDocumentRequest): SubmitAuditDocumentResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return submitAuditDocumentEx(request, headers, runtime);
}

/**
 * Description: 文档人审入审
 * Summary: 文档人审入审
 */
async function submitAuditDocumentEx(request: SubmitAuditDocumentRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SubmitAuditDocumentResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.document.submit', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryAuditDocumentRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 任务ID
  taskId: string(name='task_id'),
}

model QueryAuditDocumentResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 文档审核结果
  result?: string(name='result'),
}

/**
 * Description: 文档人审查询
 * Summary: 文档人审查询
 */
async function queryAuditDocument(request: QueryAuditDocumentRequest): QueryAuditDocumentResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryAuditDocumentEx(request, headers, runtime);
}

/**
 * Description: 文档人审查询
 * Summary: 文档人审查询
 */
async function queryAuditDocumentEx(request: QueryAuditDocumentRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryAuditDocumentResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.audit.document.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryAicoguardcloudAdbsinkRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 区域id
  regionId: string(name='region_id'),
  // DB实例id
  dbInstanceId: string(name='db_instance_id'),
  // 数据库空间名称
  nameSpace: string(name='name_space'),
  // 数据库空间密码
  nameSpacePassword: string(name='name_space_password'),
  // 数据库表名
  collectionName: string(name='collection_name'),
  // 查询的内容
  content: string(name='content'),
}

model QueryAicoguardcloudAdbsinkResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 请求的问题
  question?: string(name='question'),
  // 代答结果
  answer?: string(name='answer'),
  // 匹配度分数
  score?: string(name='score'),
  // adb的请求id
  requestId?: string(name='request_id'),
}

/**
 * Description: 阿里云ADB调用接口
 * Summary: 阿里云ADB调用接口
 */
async function queryAicoguardcloudAdbsink(request: QueryAicoguardcloudAdbsinkRequest): QueryAicoguardcloudAdbsinkResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryAicoguardcloudAdbsinkEx(request, headers, runtime);
}

/**
 * Description: 阿里云ADB调用接口
 * Summary: 阿里云ADB调用接口
 */
async function queryAicoguardcloudAdbsinkEx(request: QueryAicoguardcloudAdbsinkRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryAicoguardcloudAdbsinkResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.aicoguardcloud.adbsink.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryGuardcoreRedgptRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 唯一请求ID
  requestId: string(name='request_id'),
  // 多轮对话会话ID
  sessionId: string(name='session_id'),
  // 场景code
  sceneCode: string(name='scene_code'),
  // 调用方AppCode
  appCode: string(name='app_code'),
  // 提问内容
  question: string(name='question'),
  // 提问内容格式,当前仅支持PLAINTEXT
  questionFormat: string(name='question_format'),
  // 加密的调用方业务UserId
  userId: string(name='user_id'),
  // 是否流式输出
  stream: boolean(name='stream'),
}

model QueryGuardcoreRedgptResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // maya流式调用结果集
  data?: MayaStreamResult(name='data'),
}

/**
 * Description: 红色GPT流式调用网关接口
 * Summary: 红色GPT网关方式调用接口
 */
async function queryGuardcoreRedgpt(request: QueryGuardcoreRedgptRequest): QueryGuardcoreRedgptResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryGuardcoreRedgptEx(request, headers, runtime);
}

/**
 * Description: 红色GPT流式调用网关接口
 * Summary: 红色GPT网关方式调用接口
 */
async function queryGuardcoreRedgptEx(request: QueryGuardcoreRedgptRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryGuardcoreRedgptResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guardcore.redgpt.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryAicoguardAdbsinkRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 区域id
  regionId: string(name='region_id'),
  // DB实例id
  dbInstanceId: string(name='db_instance_id'),
  // 数据库空间名称
  nameSpace: string(name='name_space'),
  // 数据库空间密码
  nameSpacePassword: string(name='name_space_password'),
  // 数据库表名
  collectionName: string(name='collection_name'),
  // 要查询的内容
  content: string(name='content'),
}

model QueryAicoguardAdbsinkResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 请求的问题
  question?: string(name='question'),
  // 代答结果
  answer?: string(name='answer'),
  // 匹配度分数
  score?: string(name='score'),
  // adb的请求id
  requestId?: string(name='request_id'),
}

/**
 * Description: 阿里云ADB调用接口
 * Summary: 阿里云ADB调用接口
 */
async function queryAicoguardAdbsink(request: QueryAicoguardAdbsinkRequest): QueryAicoguardAdbsinkResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryAicoguardAdbsinkEx(request, headers, runtime);
}

/**
 * Description: 阿里云ADB调用接口
 * Summary: 阿里云ADB调用接口
 */
async function queryAicoguardAdbsinkEx(request: QueryAicoguardAdbsinkRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryAicoguardAdbsinkResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.aicoguard.adbsink.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model CallbackGuardAliyunRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 回执原文
  payload: string(name='payload'),
  // 回执场景，固定填写 ALIYUN"
  bizScene: string(name='biz_scene'),
}

model CallbackGuardAliyunResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 错误码
  errorCode?: string(name='error_code'),
  // 错误原因
  errorMessage?: string(name='error_message'),
}

/**
 * Description: 阿里云音频、视频的审核结果，经过 adapter 中转，通过该接口回调回来
 * Summary: 阿里云内容安全回调
 */
async function callbackGuardAliyun(request: CallbackGuardAliyunRequest): CallbackGuardAliyunResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return callbackGuardAliyunEx(request, headers, runtime);
}

/**
 * Description: 阿里云音频、视频的审核结果，经过 adapter 中转，通过该接口回调回来
 * Summary: 阿里云内容安全回调
 */
async function callbackGuardAliyunEx(request: CallbackGuardAliyunRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CallbackGuardAliyunResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.aliyun.callback', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model CallbackGuardItaskRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 回执原文
  payload: string(name='payload'),
  // 回执场景
  bizScene: string(name='biz_scene'),
}

model CallbackGuardItaskResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 错误码
  errorCode?: string(name='error_code'),
  // 错误原因
  errorMessage?: string(name='error_message'),
}

/**
 * Description: 主站ITASK审核能力回调
 * Summary: 主站ITASK审核能力回调
 */
async function callbackGuardItask(request: CallbackGuardItaskRequest): CallbackGuardItaskResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return callbackGuardItaskEx(request, headers, runtime);
}

/**
 * Description: 主站ITASK审核能力回调
 * Summary: 主站ITASK审核能力回调
 */
async function callbackGuardItaskEx(request: CallbackGuardItaskRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CallbackGuardItaskResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.itask.callback', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryGuardAudioRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 任务ID
  taskId: string(name='task_id'),
}

model QueryGuardAudioResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 音频审核结果
  result?: string(name='result'),
}

/**
 * Description: 音频人审查询
 * Summary: 音频人审查询
 */
async function queryGuardAudio(request: QueryGuardAudioRequest): QueryGuardAudioResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryGuardAudioEx(request, headers, runtime);
}

/**
 * Description: 音频人审查询
 * Summary: 音频人审查询
 */
async function queryGuardAudioEx(request: QueryGuardAudioRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryGuardAudioResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.audio.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SubmitGuardAudioRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 待检测对象的URL，请确保该URL能通过公网访问到，且URL地址长度不超过2048个字符 说明 ： URL地址中不能包含中文，且一次请求请确保仅传入1条URL
  url: string(name='url'),
  // 审核场景码，该接口固定输入BASE_AUDIO_AUDIT，其他值无效
  sceneCode: string(name='scene_code'),
  // 检测对象对应的数据ID。 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  dataId: string(name='data_id'),
  // 用于上游标识应用来源，字符串长度不能超过 128
  appCode?: string(name='app_code'),
  // 客户业务ID。 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
  // 结果通知地址，不指定时需要调用方主动查询结果
  callback?: string(name='callback'),
  // 传callback时必须指定，tenant + seed + auditResult做SHA256生成checksum，保证结果未被篡改（即数科官网控制台-账户信息中的「用户code」）
  seed?: string(name='seed'),
}

model SubmitGuardAudioResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 任务ID
  taskId?: string(name='task_id'),
  // 检测对象对应的数据ID。 如果在提交审核任务的请求参数中传入了dataId，则此处返回对应dataId
  dataId?: string(name='data_id'),
}

/**
 * Description: 音频人审入审
 * Summary: 音频人审入审
 */
async function submitGuardAudio(request: SubmitGuardAudioRequest): SubmitGuardAudioResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return submitGuardAudioEx(request, headers, runtime);
}

/**
 * Description: 音频人审入审
 * Summary: 音频人审入审
 */
async function submitGuardAudioEx(request: SubmitGuardAudioRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SubmitGuardAudioResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.audio.submit', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryGuardImageRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 任务ID
  taskId: string(name='task_id'),
}

model QueryGuardImageResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 图片审核结果
  result?: string(name='result'),
}

/**
 * Description: 图片人审查询
 * Summary: 图片人审查询
 */
async function queryGuardImage(request: QueryGuardImageRequest): QueryGuardImageResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryGuardImageEx(request, headers, runtime);
}

/**
 * Description: 图片人审查询
 * Summary: 图片人审查询
 */
async function queryGuardImageEx(request: QueryGuardImageRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryGuardImageResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.image.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SubmitGuardImageRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 待检测对象的URL，请确保该URL能通过公网访问到，且URL地址长度不超过2048个字符 URL地址中不能包含中文，且一次请求请确保仅传入1条URL
  url: string(name='url'),
  // 审核场景码，该接口固定输入BASE_IMAGE_AUDIT，其他值无效
  sceneCode: string(name='scene_code'),
  // 检测对象对应的数据ID。 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  dataId: string(name='data_id'),
  // 标识上游应用来源，字符串长度不能超过 128
  appCode?: string(name='app_code'),
  // 客户业务ID。 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
  // 结果通知地址，不指定时需要调用方主动查询结果
  callback?: string(name='callback'),
  // 传callback时必须指定，tenant + seed + auditResult做SHA256生成checksum，保证结果未被篡改（即数科官网控制台-账户信息中的「用户code」）
  seed?: string(name='seed'),
}

model SubmitGuardImageResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 任务ID
  taskId?: string(name='task_id'),
  // 检测对象对应的数据ID。 如果在提交审核任务的请求参数中传入了dataId，则此处返回对应dataId
  dataId?: string(name='data_id'),
}

/**
 * Description: 图片人审入审
 * Summary: 图片人审入审
 */
async function submitGuardImage(request: SubmitGuardImageRequest): SubmitGuardImageResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return submitGuardImageEx(request, headers, runtime);
}

/**
 * Description: 图片人审入审
 * Summary: 图片人审入审
 */
async function submitGuardImageEx(request: SubmitGuardImageRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SubmitGuardImageResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.image.submit', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryGuardTextRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 任务ID
  taskId: string(name='task_id'),
}

model QueryGuardTextResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 文本审核结果
  result?: string(name='result'),
}

/**
 * Description: 文本人审查询
 * Summary: 文本人审查询
 */
async function queryGuardText(request: QueryGuardTextRequest): QueryGuardTextResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryGuardTextEx(request, headers, runtime);
}

/**
 * Description: 文本人审查询
 * Summary: 文本人审查询
 */
async function queryGuardTextEx(request: QueryGuardTextRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryGuardTextResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.text.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SubmitGuardTextRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 待人工审核的文本内容，最长不超过10000个字符（包含中文、英文和标点符号）
  content: string(name='content'),
  // 审核场景码，固定输入BASE_TEXT_AUDIT，其他值无效
  sceneCode: string(name='scene_code'),
  // 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  dataId: string(name='data_id'),
  // 标识上游应用来源，字符串长度不能超过 128
  appCode?: string(name='app_code'),
  // 客户业务ID。 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
  // 结果通知地址，不指定时需要调用方主动查询结果
  callback?: string(name='callback'),
  // 传callback时必须指定，tenant + seed + auditResult做SHA256生成checksum，保证结果未被篡改（即数科官网控制台-账户信息中的「用户code」）
  seed?: string(name='seed'),
}

model SubmitGuardTextResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 任务ID
  taskId?: string(name='task_id'),
  // 检测对象对应的数据ID。 如果在提交审核任务的请求参数中传入了dataId，则此处返回对应dataId
  dataId?: string(name='data_id'),
}

/**
 * Description: 文本人审提交
 * Summary: 文本人审提交
 */
async function submitGuardText(request: SubmitGuardTextRequest): SubmitGuardTextResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return submitGuardTextEx(request, headers, runtime);
}

/**
 * Description: 文本人审提交
 * Summary: 文本人审提交
 */
async function submitGuardTextEx(request: SubmitGuardTextRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SubmitGuardTextResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.text.submit', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryGuardVideoRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 任务ID
  taskId: string(name='task_id'),
}

model QueryGuardVideoResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 视频审核结果
  result?: string(name='result'),
}

/**
 * Description: 视频人审查询
 * Summary: 视频人审查询
 */
async function queryGuardVideo(request: QueryGuardVideoRequest): QueryGuardVideoResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryGuardVideoEx(request, headers, runtime);
}

/**
 * Description: 视频人审查询
 * Summary: 视频人审查询
 */
async function queryGuardVideoEx(request: QueryGuardVideoRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryGuardVideoResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.video.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SubmitGuardVideoRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 待检测对象的URL，请确保该URL能通过公网访问到，且URL地址长度不超过2048个字符 URL地址中不能包含中文，且一次请求请确保仅传入1条URL
  url: string(name='url'),
  // 审核场景码，该接口固定输入BASE_VIDEO_AUDIT，其他值无效
  sceneCode: string(name='scene_code'),
  // 检测对象对应的数据ID。 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  dataId: string(name='data_id'),
  // 标识上游应用来源，字符串长度不能超过 128
  appCode?: string(name='app_code'),
  // 客户业务ID。 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
  // 结果通知地址，不指定时需要调用方主动查询结果
  callback?: string(name='callback'),
  // 传callback时必须指定，tenant + seed + auditResult做SHA256生成checksum，保证结果未被篡改（即数科官网控制台-账户信息中的「用户code」）
  seed?: string(name='seed'),
}

model SubmitGuardVideoResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 任务ID
  taskId?: string(name='task_id'),
  // 检测对象对应的数据ID。 如果在提交审核任务的请求参数中传入了dataId，则此处返回对应dataId
  dataId?: string(name='data_id'),
}

/**
 * Description: 视频人审提交
 * Summary: 视频人审提交
 */
async function submitGuardVideo(request: SubmitGuardVideoRequest): SubmitGuardVideoResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return submitGuardVideoEx(request, headers, runtime);
}

/**
 * Description: 视频人审提交
 * Summary: 视频人审提交
 */
async function submitGuardVideoEx(request: SubmitGuardVideoRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SubmitGuardVideoResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.video.submit', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryGuardAudiobaseRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 任务ID
  taskId: string(name='task_id'),
}

model QueryGuardAudiobaseResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 音频机审结果
  result?: string(name='result'),
}

/**
 * Description: 音频机审查询-基础版
 * Summary: 音频机审查询-基础版
 */
async function queryGuardAudiobase(request: QueryGuardAudiobaseRequest): QueryGuardAudiobaseResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryGuardAudiobaseEx(request, headers, runtime);
}

/**
 * Description: 音频机审查询-基础版
 * Summary: 音频机审查询-基础版
 */
async function queryGuardAudiobaseEx(request: QueryGuardAudiobaseRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryGuardAudiobaseResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.audiobase.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SubmitGuardAudiobaseRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 待检测对象的URL，请确保该URL能通过公网访问到，且URL地址长度不超过2048个字符 说明 ： 1. URL地址中不能包含中文，且一次请求请确保仅传入1条URL 2. 支持音频文件格式：MP3、WAV、AAC、WMA、OGG、M4A、AMR
  url: string(name='url'),
  // 审核场景类型 目前支持 BASE_AUDIO_SEC：音视频媒体通用检测
  sceneCode: string(name='scene_code'),
  // 检测对象对应的数据ID。 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  dataId: string(name='data_id'),
  // 标识上游应用来源，字符串长度不能超过 128
  appCode?: string(name='app_code'),
  // 客户业务ID。 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
  // 结果通知地址，不指定时需要调用方主动查询结果
  callback?: string(name='callback'),
  // 传callback时必须指定，tenant + seed + auditResult做SHA256生成checksum，保证结果未被篡改（即数科官网控制台-账户信息中的「用户code」）
  seed?: string(name='seed'),
}

model SubmitGuardAudiobaseResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 任务ID
  taskId?: string(name='task_id'),
  // 检测对象对应的数据ID。 如果在提交审核任务的请求参数中传入了dataId，则此处返回对应dataId
  dataId?: string(name='data_id'),
}

/**
 * Description: 音频机审入审-基础版
 * Summary: 音频机审入审-基础版
 */
async function submitGuardAudiobase(request: SubmitGuardAudiobaseRequest): SubmitGuardAudiobaseResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return submitGuardAudiobaseEx(request, headers, runtime);
}

/**
 * Description: 音频机审入审-基础版
 * Summary: 音频机审入审-基础版
 */
async function submitGuardAudiobaseEx(request: SubmitGuardAudiobaseRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SubmitGuardAudiobaseResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.audiobase.submit', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model ApplyGuardImagebaseRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 审核内容链接 目前支持 jpeg、png、jpg、bmp、webp 五种格式 图片的 contentType 必须为 image/xxx 或者 application/octet-stream，其余类型都无法识别
  url: string(name='url'),
  // 场景，固定填写：BASE_IMAGE_SEC
  sceneCode: string(name='scene_code'),
  // 数据Id，调用方入审数据的唯一Id
  dataId: string(name='data_id'),
  // 标识上游应用来源，字符串长度不能超过 128
  appCode?: string(name='app_code'),
  // 客户业务ID 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
}

model ApplyGuardImagebaseResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 图片机审结果
  result?: string(name='result'),
}

/**
 * Description: 图片机审申请-基础版
 * Summary: 图片机审申请-基础版
 */
async function applyGuardImagebase(request: ApplyGuardImagebaseRequest): ApplyGuardImagebaseResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return applyGuardImagebaseEx(request, headers, runtime);
}

/**
 * Description: 图片机审申请-基础版
 * Summary: 图片机审申请-基础版
 */
async function applyGuardImagebaseEx(request: ApplyGuardImagebaseRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ApplyGuardImagebaseResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.imagebase.apply', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model ApplyGuardImageadvancedRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 审核内容链接 目前支持 jpeg、png、jpg、bmp、webp 五种格式 图片的 contentType 必须为 image/xxx 或者 application/octet-stream，其余类型都无法识别
  url: string(name='url'),
  // 场景，固定填写：ADVANCED_IMAGE_SEC
  sceneCode: string(name='scene_code'),
  // 数据Id，调用方入审数据的唯一Id
  dataId: string(name='data_id'),
  // 标识上游应用来源，字符串长度不能超过 128
  appCode?: string(name='app_code'),
  // 客户业务ID 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
}

model ApplyGuardImageadvancedResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 图片审核增强版结果
  result?: string(name='result'),
}

/**
 * Description: 图片机审申请-增强版
 * Summary: 图片机审申请-增强版
 */
async function applyGuardImageadvanced(request: ApplyGuardImageadvancedRequest): ApplyGuardImageadvancedResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return applyGuardImageadvancedEx(request, headers, runtime);
}

/**
 * Description: 图片机审申请-增强版
 * Summary: 图片机审申请-增强版
 */
async function applyGuardImageadvancedEx(request: ApplyGuardImageadvancedRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ApplyGuardImageadvancedResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.imageadvanced.apply', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model ApplyGuardTextbaseRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 待审核的文本内容，最长不超过10000个字符（包含中文、英文和标点符号）
  content: string(name='content'),
  // 审核场景码，建议BASE_TEXT_SEC代指阿里云，文本审核增强版PLUS服务的某一个Service
  sceneCode: string(name='scene_code'),
  // 检测对象对应的数据ID，由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  dataId: string(name='data_id'),
  // 	
  // 标识上游应用来源，字符串长度不能超过 128
  appCode?: string(name='app_code'),
  // 客户业务ID，由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
}

model ApplyGuardTextbaseResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 文本同步审核结果，字段含义见：TextSyncAuditResult
  result?: string(name='result'),
}

/**
 * Description: 文本机审申请-基础版
 * Summary: 文本机审申请-基础版
 */
async function applyGuardTextbase(request: ApplyGuardTextbaseRequest): ApplyGuardTextbaseResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return applyGuardTextbaseEx(request, headers, runtime);
}

/**
 * Description: 文本机审申请-基础版
 * Summary: 文本机审申请-基础版
 */
async function applyGuardTextbaseEx(request: ApplyGuardTextbaseRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ApplyGuardTextbaseResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.textbase.apply', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryGuardVideobaseRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 任务ID
  taskId: string(name='task_id'),
}

model QueryGuardVideobaseResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 视频机审结果
  result?: string(name='result'),
}

/**
 * Description: 视频机审查询-基础版
 * Summary: 视频机审查询-基础版
 */
async function queryGuardVideobase(request: QueryGuardVideobaseRequest): QueryGuardVideobaseResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryGuardVideobaseEx(request, headers, runtime);
}

/**
 * Description: 视频机审查询-基础版
 * Summary: 视频机审查询-基础版
 */
async function queryGuardVideobaseEx(request: QueryGuardVideobaseRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryGuardVideobaseResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.videobase.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SubmitGuardVideobaseRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 待检测对象的URL，请确保该URL能通过公网访问到，且URL地址长度不超过2048个字符 说明 ： 1. URL地址中不能包含中文，且一次请求请确保仅传入1条URL 2. 视频文件链接支持以下协议：HTTP和HTTPS。 3. 视频文件支持以下格式：AVI、FLV、MP4、MPG、ASF、WMV、MOV、WMA、RMVB、RM、FLASH、TS。 4. 视频大小限制：默认单个视频大小不超过500 MB。如果您的需求超过500 MB，您可以对视频进行分片处理
  url: string(name='url'),
  // 审核场景类型 目前支持通用版 BASE_VIDEO_SEC
  sceneCode: string(name='scene_code'),
  // 划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  dataId: string(name='data_id'),
  // 标识上游应用来源，字符串长度不能超过 128
  appCode?: string(name='app_code'),
  // 客户业务ID。 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
  // 结果通知地址，不指定时需要调用方主动查询结果
  callback?: string(name='callback'),
  // 传callback时必须指定，tenant + seed + auditResult做SHA256生成checksum，保证结果未被篡改（即数科官网控制台-账户信息中的「用户code」）
  seed?: string(name='seed'),
}

model SubmitGuardVideobaseResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 任务ID
  taskId?: string(name='task_id'),
  // 检测对象对应的数据ID。 如果在提交审核任务的请求参数中传入了dataId，则此处返回对应dataId
  dataId?: string(name='data_id'),
}

/**
 * Description: 视频机审提交-基础版
 * Summary: 视频机审提交-基础版
 */
async function submitGuardVideobase(request: SubmitGuardVideobaseRequest): SubmitGuardVideobaseResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return submitGuardVideobaseEx(request, headers, runtime);
}

/**
 * Description: 视频机审提交-基础版
 * Summary: 视频机审提交-基础版
 */
async function submitGuardVideobaseEx(request: SubmitGuardVideobaseRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SubmitGuardVideobaseResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.videobase.submit', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model ApplyGuardImagemultiplyRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 场景，固定填写：SCENE_MULTIPLY_IMAGE_SEC
  sceneCode: string(name='scene_code'),
  // 数据Id，调用方入审数据的唯一Id
  dataId: string(name='data_id'),
  // 鉴定图片内容链接，url 和 content 参数二选一
  url?: string(name='url'),
  // 标识上游应用来源，字符串长度不能超过 128
  appCode?: string(name='app_code'),
  // 客户业务ID 由大小写英文字母、数字、下划线（_）、短划线...
  businessId?: string(name='business_id'),
  // 鉴定图片 base64 地址，url 和 content 参数二选一
  content?: string(name='content'),
}

model ApplyGuardImagemultiplyResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 光鉴检测结果
  result?: string(name='result'),
}

/**
 * Description: 用于身份证、个人信息识别、防伪等
 * Summary: 图片光鉴识别链路
 */
async function applyGuardImagemultiply(request: ApplyGuardImagemultiplyRequest): ApplyGuardImagemultiplyResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return applyGuardImagemultiplyEx(request, headers, runtime);
}

/**
 * Description: 用于身份证、个人信息识别、防伪等
 * Summary: 图片光鉴识别链路
 */
async function applyGuardImagemultiplyEx(request: ApplyGuardImagemultiplyRequest, headers: map[string]string, runtime: Util.RuntimeOptions): ApplyGuardImagemultiplyResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.imagemultiply.apply', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model CheckGuardAnswerRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 调用方标识
  appCode: string(name='app_code'),
  // 细分调用方标识，可与 appCode 传相同值
  businessId?: string(name='business_id'),
  // 文本内容，最大5000字符长度
  content: string(name='content'),
  // 场景code：
  // ● llm_output_detection：大模型输出通用场景
  sceneCode: string(name='scene_code'),
  // 是否开启流式检测功能。默认值：N：不开启
  // Y：开启
  // N：不开启
  flowDetect?: string(name='flow_detect'),
  // 会话ID，标记本次请求内容属于同一段流式内容，文本审核引擎会自动拼接后进行审核，拼接文字片段后不超过10000字的部分
  sessionId?: string(name='session_id'),
  // 是否要针对大模型输出的CoT、回答进行代答/改写。默认值：N：不开启
  // Y：开启
  // N：不开启
  replyProxy?: string(name='reply_proxy'),
  // 是否要针对大模型输出的内容中的隐私数据进行脱敏。默认值：N：不开启
  // Y：开启
  // N：不开启
  privacyDataObfuscation?: string(name='privacy_data_obfuscation'),
}

model CheckGuardAnswerResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 文本审核结果。返回结果中包含一个需自行转换的Json字符串
  result?: string(name='result'),
}

/**
 * Description: 输出文本风险检测
 * Summary: 输出文本风险检测
 */
async function checkGuardAnswer(request: CheckGuardAnswerRequest): CheckGuardAnswerResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return checkGuardAnswerEx(request, headers, runtime);
}

/**
 * Description: 输出文本风险检测
 * Summary: 输出文本风险检测
 */
async function checkGuardAnswerEx(request: CheckGuardAnswerRequest, headers: map[string]string, runtime: Util.RuntimeOptions): CheckGuardAnswerResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.answer.check', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SubmitMeiyouAuditRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 数据Id
  dataId: string(name='data_id'),
  // 模态
  modal: string(name='modal'),
  // 场景
  sceneCode: string(name='scene_code'),
  // 扩展信息
  extInfo?: string(name='ext_info'),
  // 美柚审核记录信息
  auditInfo: string(name='audit_info'),
  // 回调函数
  callback: string(name='callback'),
  // 租户
  tenantCode: string(name='tenant_code'),
}

model SubmitMeiyouAuditResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 响应结果
  result?: string(name='result'),
  // 码值
  errorCode?: string(name='error_code'),
  // 错误信息
  errorMessage?: string(name='error_message'),
  // 请求ID
  requestId?: string(name='request_id'),
}

/**
 * Description: 美柚待审核信息提交
 * Summary: 美柚待审核信息提交
 */
async function submitMeiyouAudit(request: SubmitMeiyouAuditRequest): SubmitMeiyouAuditResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return submitMeiyouAuditEx(request, headers, runtime);
}

/**
 * Description: 美柚待审核信息提交
 * Summary: 美柚待审核信息提交
 */
async function submitMeiyouAuditEx(request: SubmitMeiyouAuditRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SubmitMeiyouAuditResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.meiyou.audit.submit', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryMeiyouAgentauditRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 数据创建时间-开始时间点
  gmtCreateStart: string(pattern='\\\\d{4}[-]\\\\d{1,2}[-]\\\\d{1,2}[T]\\\\d{2}:\\\\d{2}:\\\\d{2}([Z]|([\\\\.]\\\\d{1,9})?[\\\\+]\\\\d{2}[\\\\:]?\\\\d{2})', name='gmt_create_start'),
  // 数据创建时间-结束时间点
  gmtCreateEnd: string(pattern='\\\\d{4}[-]\\\\d{1,2}[-]\\\\d{1,2}[T]\\\\d{2}:\\\\d{2}:\\\\d{2}([Z]|([\\\\.]\\\\d{1,9})?[\\\\+]\\\\d{2}[\\\\:]?\\\\d{2})', name='gmt_create_end'),
  // 所属
  owner?: string(name='owner'),
}

model QueryMeiyouAgentauditResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 响应结果
  result?: string(name='result'),
  // 码值
  errorCode?: string(name='error_code'),
  // 错误信息
  errorMessage?: string(name='error_message'),
  // 请求ID
  requestId?: string(name='request_id'),
}

/**
 * Description: 查询美柚审核Agent节点信息
 * Summary: 查询美柚审核Agent节点信息
 */
async function queryMeiyouAgentaudit(request: QueryMeiyouAgentauditRequest): QueryMeiyouAgentauditResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryMeiyouAgentauditEx(request, headers, runtime);
}

/**
 * Description: 查询美柚审核Agent节点信息
 * Summary: 查询美柚审核Agent节点信息
 */
async function queryMeiyouAgentauditEx(request: QueryMeiyouAgentauditRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryMeiyouAgentauditResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.meiyou.agentaudit.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model UpdateAicoguardcoreMeiyouRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 更新信息
  resultInfo: string(name='result_info'),
}

model UpdateAicoguardcoreMeiyouResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 响应码值
  errorCode?: string(name='error_code'),
  // 响应描述
  errorMessage?: string(name='error_message'),
}

/**
 * Description: 更新美柚审核Agent审核结果
 * Summary: 更新美柚审核Agent审核结果
 */
async function updateAicoguardcoreMeiyou(request: UpdateAicoguardcoreMeiyouRequest): UpdateAicoguardcoreMeiyouResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return updateAicoguardcoreMeiyouEx(request, headers, runtime);
}

/**
 * Description: 更新美柚审核Agent审核结果
 * Summary: 更新美柚审核Agent审核结果
 */
async function updateAicoguardcoreMeiyouEx(request: UpdateAicoguardcoreMeiyouRequest, headers: map[string]string, runtime: Util.RuntimeOptions): UpdateAicoguardcoreMeiyouResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.aicoguardcore.meiyou.update', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model SubmitGuardDocumentRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 待检测对象的URL，请确保该URL能通过公网访问到，且URL地址长度不超过2048个字符
  // 说明 ：
  // URL地址中不能包含中文，且一次请求请确保仅传入1条URL
  url: string(name='url'),
  // 审核场景码
  sceneCode: string(name='scene_code'),
  // 检测对象对应的数据ID。
  // 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  dataId: string(name='data_id'),
  // 标识上游应用来源，字符串长度不能超过 128
  appCode?: string(name='app_code'),
  // 客户业务ID。
  // 由大小写英文字母、数字、下划线（_）、短划线（-）、英文句号（.）组成，不超过128个字符，可以用于唯一标识您的业务数据
  businessId?: string(name='business_id'),
  // 结果通知地址，不指定时需要调用方主动查询结果
  callback?: string(name='callback'),
  // 传callback时必须指定，tenant + seed + auditResult做SHA256生成checksum，保证结果未被篡改（即数科官网控制台-账户信息中的「用户code」）
  seed?: string(name='seed'),
}

model SubmitGuardDocumentResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 任务ID
  taskId?: string(name='task_id'),
  // 检测对象对应的数据ID。
  // 说明：
  // 如果在提交审核任务的请求参数中传入了dataId，则此处返回对应dataId
  dataId?: string(name='data_id'),
}

/**
 * Description: 文档人审入审
 * Summary: 文档人审入审
 */
async function submitGuardDocument(request: SubmitGuardDocumentRequest): SubmitGuardDocumentResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return submitGuardDocumentEx(request, headers, runtime);
}

/**
 * Description: 文档人审入审
 * Summary: 文档人审入审
 */
async function submitGuardDocumentEx(request: SubmitGuardDocumentRequest, headers: map[string]string, runtime: Util.RuntimeOptions): SubmitGuardDocumentResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.document.submit', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QueryGuardDocumentRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 任务ID
  taskId: string(name='task_id'),
}

model QueryGuardDocumentResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 文档审核结果
  result?: string(name='result'),
}

/**
 * Description: 文档人审查询
 * Summary: 文档人审查询
 */
async function queryGuardDocument(request: QueryGuardDocumentRequest): QueryGuardDocumentResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return queryGuardDocumentEx(request, headers, runtime);
}

/**
 * Description: 文档人审查询
 * Summary: 文档人审查询
 */
async function queryGuardDocumentEx(request: QueryGuardDocumentRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QueryGuardDocumentResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.guard.document.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QuerySecurityQuestionRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 调用方唯一标示
  enterprise?: string(name='enterprise'),
  // 提问内容
  question: string(maxLength=10000, name='question'),
  // 调用方标示
  businessId: string(name='business_id'),
  // 场景code
  sceneCode: string(name='scene_code'),
  // 标示是否是同一个Q&A
  messageId?: string(name='message_id'),
  // 会话ID
  sessionId?: string(name='session_id'),
  // 是否开启流式检测功能。默认值：N：不开启，Y：开启
  multiSessionDetect?: string(name='multi_session_detect'),
  // 是否开启针对大模型输入文本的的安全改写和增强功能。默认值：N：不开启，Y：开启
  promptReword?: string(name='prompt_reword'),
  // 是否需要针对提问内容的进行金融合规检测。默认值：N：不开启，Y：开启
  financeComplianceDetection?: string(name='finance_compliance_detection'),
  // 是否需要针对提问内容的进行领域识别
  fieldIdentify?: string(name='field_identify'),
  // 是否开启提示词攻击防御功能
  promptAttackDefense?: string(name='prompt_attack_defense'),
  // 是否开启隐私数据泄露的专项检测
  privacyDataDetection?: string(name='privacy_data_detection'),
  // 是否开启安全代答功能
  securityAnswer?: string(name='security_answer'),
}

model QuerySecurityQuestionResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 代表风险等级和建议的处置结论，PASS：安全无风险，BLOCK：绝对风险，建议直接拦截，SECURITY_ANSWER：泛风险，回答内容存在敏感要素
  actionCode?: string(name='action_code'),
  // 命中的自定义黑词列表
  customizeRiskWord?: [ string ](name='customize_risk_word'),
  // 风险标签
  labels?: [ LabelModel ](name='labels'),
  // 会话Id
  sessionId?: string(name='session_id'),
  // 表示是同一个Q&A
  messageId?: string(name='message_id'),
  // 兜底话术
  limitAnswer?: string(name='limit_answer'),
  // 有风险时的安全代答
  securityAnswer?: string(name='security_answer'),
  // 当请求参数 promptReword=Y 时返回，为安全改写后的内容
  securityPrompt?: string(name='security_prompt'),
  // 提示词攻击手法标签
  attackLabels?: AttackLabel(name='attack_labels'),
  // 领域标签信息
  fieldInfo?: FieldModel(name='field_info'),
}

/**
 * Description: 天鉴SaaS提问检测
 * Summary: 天鉴SaaS提问检测
 */
async function querySecurityQuestion(request: QuerySecurityQuestionRequest): QuerySecurityQuestionResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return querySecurityQuestionEx(request, headers, runtime);
}

/**
 * Description: 天鉴SaaS提问检测
 * Summary: 天鉴SaaS提问检测
 */
async function querySecurityQuestionEx(request: QuerySecurityQuestionRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QuerySecurityQuestionResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.security.question.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

model QuerySecurityAnswerRequest = {
  // OAuth模式下的授权token
  authToken?: string(name='auth_token'),
  productInstanceId?: string(name='product_instance_id'),
  // 调用标示
  enterprise?: string(name='enterprise'),
  // 细分调用方标识
  businessId: string(name='business_id'),
  // 当前回答内容，最大长度40000个字符
  content: string(maxLength=40000, name='content'),
  // 场景code
  sceneCode: string(name='scene_code'),
  // 是否开启流式检测功能。默认值：N：不开启，Y：开启
  flowDetect?: string(name='flow_detect'),
  // 会话id
  flowMsgId?: string(name='flow_msg_id'),
  // 流失内容结束标示
  flowEnd?: string(name='flow_end'),
  // 表示是同一个Q&A
  messageId?: string(name='message_id'),
  // 是否要针对大模型输出的内容中的隐私数据进行脱敏。默认值：N：不开启，Y：开启
  privacyDataObfuscation?: string(name='privacy_data_obfuscation'),
  // 是否开启隐私数据泄露的专项检测（新字段，老privacy废弃）
  privacyDataDetection?: string(name='privacy_data_detection'),
}

model QuerySecurityAnswerResponse = {
  // 请求唯一ID，用于链路跟踪和问题排查
  reqMsgId?: string(name='req_msg_id'),
  // 结果码，一般OK表示调用成功
  resultCode?: string(name='result_code'),
  // 异常信息的文本描述
  resultMsg?: string(name='result_msg'),
  // 代表风险等级和建议的处置结论，PASS：安全无风险，BLOCK：绝对风险，建议直接拦截，SECURITY_ANSWER：泛风险，回答内容存在敏感要素
  actionCode?: string(name='action_code'),
  // 风险标签
  labels?: [ LabelModel ](name='labels'),
  // 命中的自定义黑词列表
  customizeRiskWord?: [ string ](name='customize_risk_word'),
  // 针对流式输入的处理状态，针对流式输入的处理状态，当请求参数 flowDetect=Y 时返回，结果信息如下：
  // 1. processing：等待处理中，暂无风险检测结果，可能是输入的文本信息不足一句
  // 2. done：处理完成，请参考actionCode及对应的风险标签信息
  flowProcessState?: string(name='flow_process_state'),
  // 流式内容ID
  flowMsgId?: string(name='flow_msg_id'),
  // 表示是同一个Q&A
  messageId?: string(name='message_id'),
  // 兜底话术
  securityAnswer?: string(name='security_answer'),
}

/**
 * Description: 天鉴SaaS回答检测
 * Summary: 天鉴SaaS回答检测
 */
async function querySecurityAnswer(request: QuerySecurityAnswerRequest): QuerySecurityAnswerResponse {
  var runtime = new Util.RuntimeOptions{};
  var headers : map[string]string= {};
  return querySecurityAnswerEx(request, headers, runtime);
}

/**
 * Description: 天鉴SaaS回答检测
 * Summary: 天鉴SaaS回答检测
 */
async function querySecurityAnswerEx(request: QuerySecurityAnswerRequest, headers: map[string]string, runtime: Util.RuntimeOptions): QuerySecurityAnswerResponse {
  Util.validateModel(request);
  return doRequest('1.0' ,'aitech.comm.security.answer.query', 'HTTPS', 'POST', `/gateway.do`, request, headers, runtime);
}

